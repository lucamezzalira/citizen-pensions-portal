Architecture Decision Records (ADRs) capture the significant
architectural choices that constrain this codebase, including the
context that led to each decision and the consequences that follow
from it. Read the ADRs whose `touches` paths match your task before
writing feedforward artifacts; they are load-bearing and must not
be contradicted.
