The entitlements domain owns
the pension itself: the calculation basis, current monthly amount,
payment history, and adjustments over time. Every displayed amount
must be reproducible from stored inputs per ADR 0003, and the
calculation rules are versioned so historical decisions remain
reproducible. This domain does not initiate payments; it displays
what has been calculated and paid.
